
.ba/triggers/
.ba/locks/

# dependencies
node_modules/

# next.js
.next/
out/

# builds / caches
*.tsbuildinfo

# env — secrets never committed
.env
.env.*
!.env.example

# drizzle migration artifacts are committed; local db dumps are not
*.dump

# local waitlist lead fallback (dev without DATABASE_URL)
.data/

# misc
.DS_Store
dist/
coverage/
*.log

# R16 design bundle: prototypes + README are tracked; assets/ duplicate public/assets
design_handoff_r16_landing/assets/
