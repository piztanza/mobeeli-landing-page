# HANDOFF_GEMINI_TASTE_SWEEP.md — 18 ranked proposals from the six-lens research swarm

Written 2026-07-23 | Source: Fable's six-lens research swarm (dark-cinematic, marketplace-trust,
trends-2026, SEA-local, micro-craft, conversion-craft — 6 researchers + 1 synthesis, every
recipe verified against live sites). This supersedes the generic hypothesis queue: execute
from THIS list. All prior contracts hold (CLAUDE.md, no new deps, EN+ID founder-stamped copy,
reduced-motion, LCP < 2.5s, no moat stats on `/`, brand = Mobeeli, no emerald/indigo).

## Execution rules
- One proposal per iteration; full gate (`npm test` → `lint` → `build`); uncommitted handback
  for Fable's audit (the round-2 protocol that worked).
- ⛔ = blocked on a founder decision — do NOT implement until stamped.
- Numbers rule (absolute): every count/stat shown must be real or explicitly labeled
  Simulasi/Simulation. No fabricated queue positions, batch fills, or telemetry.
- Copy rule: existing approved values are never edited; new strings are new EN+ID keys.

## Immediate queue (no founder gate — start here, in order)

**Q1. Cursor-tracked glow-border card system** (rank 3, wow 9, effort low) — supersedes the
old border-glow hypothesis. One delegated pointermove listener per section writes --mx/--my
(rAF, no React re-render); cards' ::after carries a 1px #5b9bf7 border masked to
`radial-gradient(ellipse 200px 200px at var(--mx) var(--my), #000 0%, #0009 30%, #0003 50%, transparent 70%)`;
glow travels across sibling cards. Hero floating cards get the fill variant
(600px circle rgba(91,155,247,.2)). Gate `@media (hover:hover)`. Source: linear.app
`.MwJdiW_shine` (verified live CSS), raycast.com.

**Q2. Dark-band material system** (rank 6, wow 8, low) — elevation tokens off ink
(#0a1019 / #111927 / #16202f, hairlines #ffffff14/#ffffff1f); Linear 4-layer card shadow
`inset 0 0 0 1px #ffffff08, inset 0 1px #ffffff0a, 0 0 0 1px #0009, 0 4px 4px #0000001a`;
Resend top-spotlight on sprite cards; Raycast film grain ::after (data-URI feTurbulence,
opacity .07, ~300 bytes) on both dark bands — also kills banding on the Jakarta video.

**Q3. Scroll-illuminated problem statement** (rank 4, wow 9, low) — apply to the EXISTING
`prob_h2` copy (no rewrite = no stamp needed): server-rendered per-word spans, words brighten
.18→1 via `animation-timeline: view(); animation-range: entry 40% cover 55%` staggered 2%/word;
`@supports` gate (Firefox renders opaque); zero JS, off-main-thread.

**Q4. Site-wide motion standard** (rank 15, wow 7, low) — entrance token 480ms
cubic-bezier(.19,1,.22,1), 16px max rise, 70ms stagger; Stripe HoverArrow on secondary links;
left-in/right-out link underline (background-size trick, survives wrapped ID strings); button
press spring on an inner span (`linear(0, 1.25, 1, .9, 1.04, .99, 1.005, .996, 1.001, .999, 1)`);
double focus ring `0 0 0 2px var(--ring-gap), 0 0 0 4px #2f7df6` — must not fight the existing
:focus-visible system, extend it.

**Q5. Typography display math** (rank 5 part 1, wow 8, trivial) — headings to weight 600,
H1 `clamp(2.5rem, 6vw, 4.5rem)` lh 1.0–1.06, letter-spacing -0.022em ≥2rem / -0.012em below.
(Part 2 — Plus Jakarta Sans stylistic sets ss01–ss03 — needs a devtools visual test first;
if Google Fonts strips features, self-hosting the headings TTF is its own iteration.)

## Founder-gated proposals (⛔ stamp required — presented for decision)

| # | Proposal (rank, wow) | The decision needed |
|---|---|---|
| G1 | **YMM simulator as lit app window + Indonesian personas + rolling odometer** (1, 10) | Personas ('Pak Budi — Toyota Avanza 1.3 G 2016 — kampas rem depan'…) and narrowing counts must map to real catalog rows |
| G2 | **Jakarta-signage bilingual lockup** (2, 9) — Indonesian-primary headings with small EN caption, MRT-wayfinding style | Brand position: Indonesian-first hierarchy for an investor audience? (Inverts current EN-primary + toggle) |
| G3 | **Archipelago → network proof** (7, 9) — Sabang/Merauke pulse markers ('every Indonesian completes the song line'), corridor arcs, bottom dissolve mask | Corridor cities; any shipment rows must be labeled Simulasi |
| G4 | **Kinetic hero H1 ticker + variable-weight inflate** (8, 8) | Colloquial Bahasa register ('Cari onderdil? Pasti ketemu.') + noun list |
| G5 | **Hero card authenticity pass** (9, 7) — Rp185.000 formatting, Airbnb-spec verified badge, Bahasa part names | Touches existing approved copy values (card_part_price format) — founder must bless the value edits |
| G6 | **Buyer strip rebuild** (10, 8) — fused email pill, second real quote card, WhatsApp secondary (site's only green #25D366), single CTA verb everywhere | Second real shopkeeper quote + official +62 number |
| G7 | **/join queue mechanics** (11, 8) — 2-field step 1, real queue-position count-up, WA share | Truthful access window; count must be the real row count |
| G8 | **Batch capacity meter** (12, 8) — /early-adopters only, never on `/` | The 50-slot Batch 1 cap must be a real operational decision |
| G9 | **AI-catalog bento restructure** (13, 8) | Choose 3–4 TRUE micro-proof cells |
| G10 | **Sticky scrollytelling how-it-works** (14, 9, HIGH effort) — scroll-scrubbed simulator states | Replaces auto-cycle pacing; composes with G1 — do after G1 |
| G11 | **Indonesian trust chrome** (16, 7) — 'Karya anak bangsa' chip + CSS Merah-Putih flag, diligence footer (PT entity, address, 'Data Anda tidak pernah dijual'), reserved PSE badge slot | PT legal entity + publishable address; PSE badge only after real registration |
| G12 | **Testimonial BukuWarung anatomy** (17, 7) — photo, 'Pemilik Bengkel X, [city]', dulu/sekarang shape, 'Juragan' address on /early-adopters | Shopkeeper photo permission + wording verification |
| G13 | **Investor letter + footnoted count-up tiles** (18, 7) — /investors + /why-mobeeli only | Founder writes/signs the letter; every stat needs source + date footnote |

## Discarded on taste (do not resurrect)
Phone-framed hero video (duplicates simulator) · hero search pill (dead-end pre-launch) ·
conic border-beams & shine-sweep CTAs (Magic-UI default) · H1 shimmer (effect-stacking) ·
Vercel '+' grid guides (pastiche) · infinite marquees (2022 default) · 3D card tilt
(most-copied effect of the decade) · three-benefit trio strip (generic + unverifiable) ·
View Transitions API (experimental flag on investor-facing site) · sticky bottom mobile CTA
(pushy for this audience) · free-shipping claims (not true yet).

## Also in flight (Fable-owned)
Platform repo branch `feat/mobeeli-brand-security` (pushed): Mobeeli rename on /platform,
analytics auth-wall, 0% hero pill. Remaining there: emerald→blue recolor of /platform (big
sweep, own iteration), stock-photo batches (founder downloads approval pending).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
